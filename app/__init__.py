"""QuickCommerce India — AI commerce aggregation assistant.

Modules map 1:1 to directories under app/ per the 11-module harness pattern.
This is a runnable vertical slice: bootstrap -> connectors -> memory (claim
graph) -> engine (normalize/compare/recommend) -> llm -> security (HITL) -> web.
"""

__version__ = "0.1.0"
